// Statements in C denote its end by ';'
// A group of statements are denoted under a say a fn, are denoted by enclosed { }. They are called compound statements or block.

// a loop table cmp in C instead of linear cmps:
    //switch
    // syn:
    /*
    switch (expression){
        case exp:
        default:
    }
    */

// under a looping control there is break and continue to interupt a loop

// Goto 
    // A special control only statement used to force control over to a specified label. Labels are of local memory.