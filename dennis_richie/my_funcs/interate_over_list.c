#include <stdio.h>
/*
char * iterfunc(char fn[], int size, int from, int to){
    static char input[10];
    int j = 0;
    for(int i = from; i<size && i < to; i++){
        input[j] = fn[i];
        j++;
    }
    return input;
}

int main(){
    char list1[] = {'a', 'b', '\0'};
    printf("output: %d", list1[-1]);
    printf("output: %s", iterfunc(list1, 3, 0, 3));
}
*/
