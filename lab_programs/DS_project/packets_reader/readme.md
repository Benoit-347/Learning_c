# DS_temporary_repo
C Packet Capture Filter Engine
What we can observe:
    ✔ IP parsing
    ✔ TCP/UDP port logic
    ✔ Filter correctness
    ✔ Endianness handling
    ✔ Real network latency

Usage:   ./pktcap -i <interface> [-f "filter"] [-r <file>]

Only sees ur PC's loopover/ wireless/ ethernet traffic
